---
apiVersion: azure.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: aks-import
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: default
      name: azure-account-creds
      key: credentials
